{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "git": {
    "deploymentEnabled": false
  },
  "ignoreCommand": "if git diff --name-only HEAD^ HEAD | grep -qEv '^(docs/|app/design-plan-evidence\\.json)$'; then exit 1; else exit 0; fi"
}
